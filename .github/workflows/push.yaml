name: Push
on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '**'
jobs:
  call-test:
    uses: bluegosolutions/gl-gh-actions-workflows/.github/workflows/test.yaml@0.4.1
    secrets:
      username: ${{ secrets.IMAGE_GITHUB_USERNAME }}
      password: ${{ secrets.IMAGE_GITHUB_PASSWORD }}
    with:
      enable_qemu: true
